# 텅텅 (TangTang) 프론트엔드 개발 실행 계획서 (plan.md)

**프로젝트명:** 텅텅(TangTang) - 재차인원 기반 서울 통근 혼잡회피 서비스  
**작성일:** 2026.08.19 (v1.0)  
**개발 기간:** 2026.08.18 ~ 2026.08.21 (총 4일 스프린트)  
**담당자:** 윤상은 (프론트엔드 개발)

---

## 1. 프로젝트 개요 및 프론트엔드 목표
- **프로젝트 핵심:** 대량 하차 직후의 실제 재차인원을 기반으로, 덜 혼잡한 지점 및 시간을 이용하는 경로를 추천하는 서비스.
- **핵심 UX 플로우 (3-Step):** `출발지/도착지 입력` → `추천 경로 목록 확인` → `지도에서 경로 시각화 확인`.
- **주요 제약사항:**
  - **비회원 서비스 (N-04):** 회원가입, 로그인, 마이페이지, 예매 내역 없음.
  - **데이터 보존 minimal:** 최근 검색 기록 등 사용자 개인정보는 영구 저장하지 않음 (브라우저 세션 상태로만 유지).
  - **비즈니스 로직 분리:** 혼잡 스코어링, ODsay API 연동, 가중치 계산은 백엔드 담당. 프론트는 데이터 시각화 및 UX/UI 구현에 집중.

---

## 2. 기술 스택 (Tech Stack)

| 구분 | 선택 기술 | 비고/사유 |
| :--- | :--- | :--- |
| **Framework** | React 18 + TypeScript | 타이트한 일정 고려 생산성 및 타입 안정성 확보 |
| **Build Tool** | Vite | 빠른 HMR 개발 환경 제공 |
| **Styling** | TailwindCSS | 유틸리티 클래스 기반 rapid UI/카드/배지 스타일링 |
| **State / Async** | React Query (TanStack Query) + Axios | 백엔드 API 연동, 로딩/에러 상태 관리 (N-01 대응) |
| **Map SDK** | 카카오맵 JavaScript SDK | 국내 정류장/주소 검색 편의성 및 무료 쿼터 제공 |

---

## 3. 백엔드 API 연동 계약 및 요청사항

### 3.1 주요 API Endpoint

- `POST /routes/search`: 출발지/도착지/기준시간 입력 후 경로 후보 목록 조회 (메인 검색)
- `GET /routes/{request_id}`: 이전 요청 재조회 (뒤로가기/새로고침 시 상태 복원용)
- `GET /admin/batch/latest`: 최신 배치 실행 상태 (UI 하단 '데이터 기준일' 표시용)
- `GET /health`: 백엔드 서버 연결 및 헬스체크

### 3.2 백엔드 협업 사전 확정 필요사항 [CRITICAL]
- **`segments` 내 좌표(lat/lng) 포함 요청:**
  - 현재 응답 스키마는 `station_id`, `stop_id`만 전달되어 프론트에서 지도 시각화 시 추가 조회가 필요함.
  - 프론트 추가 API 호출 없이 바로 지도를 그릴 수 있도록 **`segments` 각 항목에 `start` 및 `end` 위경도 좌표 포함**을 정종우·김창영님께 요청 및 확정 필수.

### 3.3 에러 코드별 UI/UX 처리 정책

| Http Status / Code | 의미 | UI 처리 방식 |
| :--- | :--- | :--- |
| **400 INVALID_INPUT** | 입력값 오류 | 입력 폼 에러 메시지 노출 ("출발지를 다시 선택해 주세요") |
| **404 NO_CANDIDATE** | 추천 경로 없음 | Empty State 화면 노출 ("추천 경로를 찾지 못했습니다") |
| **429 ODSAY_QUOTA_EXCEEDED** | 일일 쿼터 초과 | 시스템 안내 얼럿 ("잠시 후 다시 시도해 주세요") |
| **502 UPSTREAM_ERROR** | 연동 시스템 오류 | 에러 안내 화면 + '재시도' 버튼 제공 |

---

## 4. 화면 및 기능 구성 상세 (UI Components)

### 4.1 화면 ①: 경로 검색 화면 (Search Screen)
- **주요 기능 (F-01):**
  - 출발지 / 도착지 입력 (주소 검색 or 지도 클릭 인터랙션)
  - 기준 시간 선택 (기본값: 현재 시간 `departure_time`)
  - 검색 실행 버튼 (`POST /routes/search` 호출)
  - **N-01 적용:** 검색 버튼 클릭 시 로딩 스피너/스켈레톤 UI 표시.

### 4.2 화면 ②: 추천 경로 결과 목록 (Result List Screen)
- **주요 기능 (F-02, F-03, F-04):**
  - 후보 경로 카드 목록 출력
  - **정보 항목:** 전체 소요시간 (`total_time_min`), 혼잡도 스코어 (`congestion_score`), 구간별 이동 모드
  - **시각화 배지:**
    - `is_recommended: true` 및 `minute_improvement_ratio` 상위 항목에 **"추천" 배지** 부착
    - 자전거 수단 포함 여부 표시
    - `congestion_score` 기반 혼잡도 컬러 배지 (여유/보통/혼잡)

### 4.3 화면 ③: 경로 상세 및 지도 시각화 (Detail & Map Screen)
- **주요 기능 (F-06):**
  - 카카오맵 SDK 기반 지도 시각화
  - 선택된 경로의 Polyline 및 Marker 바인딩
  - 하단/측면 구간별 상세 타임라인 (도보/지하철/버스/자전거 아이콘 및 소요시간)

---

## 5. 지도 시각화 구현 방안 (Map Visualization)

1. **지하철 / 버스 구간:**
   - `segments`로 수신한 좌표(또는 역/정류장 위치)를 순서대로 이은 Polyline 생성.
   - 이동 수단별 Line Color 및 Stroke Style 다변화 (예: 지하철 노선색, 버스 색상).
2. **도보 / 자전거 구간 (1차 MVP 방식):**
   - 세부 보행자/자전거 폴리라인 미확정 상태이므로, 시작점과 끝점을 이은 **직선 Polyline(점선)으로 우선 구현**.
3. **따릉이 대여소 (F-05):**
   - 지도 상 대여소 위치 마커 표시. (실시간 재고 배지는 백엔드 API 완성 후 추가 연동).

---

## 6. 스프린트 일정 및 체크리스트 (4-Day Timeline)

```
[Day 1: 08.18] ──> [Day 2: 08.19 (오늘)] ──> [Day 3: 08.20] ──> [Day 4: 08.21]
  기획/환경세팅        UI 구현 & 스키마 확정      API 연동 & 지도    통합테스트 & 데모
```

### 6.1 착수 전 체크리스트 (Pre-flight Checklist)
- [ ] 카카오맵 API 키 발급 및 `.env` 파일 설정 (`VITE_KAKAO_MAP_KEY`)
- [ ] 백엔드 팀(정종우, 김창영)과 `segments` 좌표 포함 스키마 최종 확정
- [ ] 백엔드 Base URL 공유 받기 및 환경변수 설정 (`VITE_API_BASE_URL`)

### 6.2 일자별 상세 실행 계획

#### **08.18 (Day 1) - 기본 구조 설계 [완료/진행중]**
- [x] 개발 환경 세팅 (Vite + React + TS + TailwindCSS)
- [x] 카카오맵 SDK 라이브러리 선정
- [x] 화면 3종 와이어프레임 구조 잡기

#### **08.19 (Day 2) - UI 컴포넌트 개발 & 인터페이스 확정 [오늘]**
- [ ] 백엔드와 `segments` 좌표 전달 방식 계약 확정
- [ ] **검색 화면 (Search Form)** 구현 (출발/도착/시간 입력)
- [ ] **결과 목록 화면 (Candidate List)** 컴포넌트 구현 (더미 데이터 바인딩)
- [ ] 혼잡도 배지 및 추천 알고리즘 결과 시각화 UI 개발

#### **08.20 (Day 3) - API 연동 & 지도 시각화**
- [ ] 백엔드 `POST /routes/search` API 실제 연동 및 React Query 처리
- [ ] Axios Interceptor / Error Boundary 구축 (400, 404, 429, 502 예외 처리)
- [ ] 카카오맵 위에 경로 Polyline / Marker 렌더링 기능 개발 (버스/지하철/도보)
- [ ] N-01 대응: 로딩 상태 처리 (Skeleton UI 및 로딩 스피너)

#### **08.21 (Day 4) - 통합 테스트 & 데모 준비**
- [ ] 전체 E2E 플로우 테스트 (`검색 -> 결과 선택 -> 지도 확인`)
- [ ] 새로고침 및 뒤로가기 시 `GET /routes/{request_id}` 복원 연동
- [ ] 반응형 UI 점검 및 자잘한 스타일 다듬기
- [ ] 데모 시나리오 준비 및 최종 배포

---

## 7. 팀 협업 및 커뮤니케이션 규칙
- **백엔드 (정종우, 김창영):** API 응답 스키마 변경 발생 시 즉시 공유 프로세스 가동.
- **인프라/DB (김재우):** API 배포 URL 공유받기 및 정류장/역 좌표 인덱스 관련 필요시 문의.
- **개인정보 보호 (N-04):** 브라우저 LocalStorage에 검색 내역을 영구 저장하는 코드 금지.
